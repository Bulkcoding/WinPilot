<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- DevExpress GroupControl 느낌의 Border 기반 그룹 패널: 제목 영역 + 구분선 + Content 영역 -->
    <Style x:Key="GroupPanelStyle" TargetType="HeaderedContentControl">
        <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="HeaderedContentControl">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <!-- 제목 영역 -->
                            <ContentPresenter Grid.Row="0" ContentSource="Header" Margin="18,14,18,10"/>

                            <!-- 구분선 -->
                            <Rectangle Grid.Row="1" Height="1" Fill="{TemplateBinding BorderBrush}"/>

                            <!-- Content 영역 -->
                            <ContentPresenter Grid.Row="2" Margin="18"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- 그룹 패널 제목 텍스트 -->
    <Style x:Key="GroupTitleStyle" TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="15"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
    </Style>

    <!-- 그리드 헤더/행/셀 스타일은 App.xaml의 공용 DataGridHeaderStyle/RowStyle/CellStyle을 사용한다.
         (규칙은 CLAUDE.md의 "DataGrid 스타일 규칙" 참고) -->

</ResourceDictionary>
