<UserControl x:Class="WinPilot.Views.UtilitiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:WinPilot.Views"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
        <ResourceDictionary.MergedDictionaries>
            <ResourceDictionary Source="/WinPilot;component/Styles/GroupPanelStyles.xaml"/>
        </ResourceDictionary.MergedDictionaries>

        <!-- 탭 스타일 (파서 탭과 동일) -->
        <Style x:Key="UtilTabStyle" TargetType="TabItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="Bd" Padding="{TemplateBinding Padding}"
                                BorderThickness="0,0,0,2" BorderBrush="Transparent">
                            <TextBlock x:Name="Tb" Text="{TemplateBinding Header}"
                                       Foreground="{TemplateBinding Foreground}"
                                       FontSize="{TemplateBinding FontSize}"
                                       FontWeight="SemiBold"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#F97316"/>
                                <Setter TargetName="Tb" Property="Foreground"   Value="#F97316"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Tb" Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ErrorText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#EF4444"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,4,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>

        <Style x:Key="AreaLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,6"/>
        </Style>

        <Style x:Key="IconBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ControlBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CodeBox" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource ConsoleBgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ConsoleTextBrush}"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextPrimaryBrush}"/>
        </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Background="{DynamicResource BgBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <DockPanel Grid.Row="0" Margin="24,20,24,0">
            <TextBlock Text="Utiles" FontSize="22" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"/>
        </DockPanel>

        <!-- 탭 컨트롤 -->
        <TabControl Grid.Row="1" Margin="24,12,24,24"
                    Background="Transparent" BorderThickness="0" Padding="0">
            <TabControl.Resources>
                <Style TargetType="TabPanel">
                    <Setter Property="Background" Value="Transparent"/>
                </Style>
            </TabControl.Resources>
            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TabPanel Grid.Row="0" IsItemsHost="True" Background="Transparent"/>
                        <Rectangle Grid.Row="1" Height="1"
                                   Fill="{DynamicResource BorderBrush}" Margin="0,0,0,16"/>
                        <ContentPresenter Grid.Row="2" ContentSource="SelectedContent"/>
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>

            <!-- ═══ 검진 ═════════════════════════════════════════ -->
            <TabItem Header="검진" Style="{StaticResource UtilTabStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1*" MinWidth="260"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="3*" MinWidth="360"/>
                    </Grid.ColumnDefinitions>

                    <!-- ── 좌측: 검진 종류 / 검사 항목 / SamFile 그룹 패널 ── -->
                    <Grid Grid.Column="0" Margin="0,0,10,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- 검진 종류 / 검사 항목 (연동 콤보박스) -->
                        <StackPanel Grid.Row="0" Margin="0,0,0,12">
                            <StackPanel Margin="0,0,0,10">
                                <TextBlock Text="검진 종류" Style="{StaticResource AreaLabel}"/>
                                <ComboBox Style="{StaticResource FilterComboStyle}"
                                          ItemsSource="{Binding Categories}"
                                          SelectedItem="{Binding SelectedCategory}"/>
                            </StackPanel>
                            <StackPanel>
                                <TextBlock Text="검사 항목" Style="{StaticResource AreaLabel}"/>
                                <ComboBox Style="{StaticResource FilterComboStyle}"
                                          ItemsSource="{Binding SubTypes}"
                                          SelectedItem="{Binding SelectedSubType}"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- SamFile 그룹 패널 (제목 영역 + Content 영역 구분) -->
                        <HeaderedContentControl Grid.Row="1" Style="{StaticResource GroupPanelStyle}" Margin="0,0,0,8">
                            <HeaderedContentControl.Header>
                                <DockPanel>
                                    <Button DockPanel.Dock="Right" Content="지우기"
                                            Command="{Binding ClearCheckupCommand}"
                                            Style="{StaticResource IconBtnStyle}"/>
                                    <TextBlock Text="SamFile" Style="{StaticResource GroupTitleStyle}"/>
                                </DockPanel>
                            </HeaderedContentControl.Header>
                            <TextBox Style="{StaticResource CodeBox}" MinHeight="60"
                                     Text="{Binding CheckupInput, UpdateSourceTrigger=PropertyChanged}"/>
                        </HeaderedContentControl>

                        <!-- 버튼 -->
                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Button Content="▶  정리" Command="{Binding ParseCheckupCommand}"
                                    Style="{StaticResource PrimaryButtonStyle}" Padding="20,7" Margin="0,0,8,0"/>
                            <TextBlock Text="{Binding CheckupError}" Style="{StaticResource ErrorText}"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <!-- 좌/우 패널 크기 조절 스플리터 -->
                    <GridSplitter Grid.Column="1" Width="6" Margin="2,0"
                                  HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                                  Background="Transparent"
                                  ResizeBehavior="PreviousAndNext" ShowsPreview="True"/>

                    <!-- ── 우측: 정리 결과 그룹 패널 ── -->
                    <HeaderedContentControl Grid.Column="2" Style="{StaticResource GroupPanelStyle}">
                        <HeaderedContentControl.Header>
                            <DockPanel>
                                <Button DockPanel.Dock="Right" Content="복사"
                                        Command="{Binding CopyCheckupCommand}"
                                        Style="{StaticResource IconBtnStyle}"/>
                                <TextBlock Text="정리 결과" Style="{StaticResource GroupTitleStyle}"/>
                            </DockPanel>
                        </HeaderedContentControl.Header>

                        <!-- 체크박스 컬럼 없음, 행 단위 선택/드래그, 얇은 구분선, 가로 스크롤 -->
                        <DataGrid ItemsSource="{Binding CheckupFields}"
                                  AutoGenerateColumns="False" HeadersVisibility="Column"
                                  SelectionUnit="FullRow" SelectionMode="Extended"
                                  CanUserAddRows="False" CanUserDeleteRows="False"
                                  CanUserResizeRows="False" IsReadOnly="True"
                                  GridLinesVisibility="Horizontal"
                                  Background="Transparent" BorderThickness="0"
                                  RowBackground="Transparent"
                                  HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                  FontSize="13"
                                  HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Auto"
                                  ColumnHeaderStyle="{StaticResource DataGridHeaderStyle}"
                                  RowStyle="{StaticResource DataGridRowStyle}"
                                  CellStyle="{StaticResource DataGridCellStyle}">
                            <!-- SelectionUnit=FullRow에서는 DataGridRow의 기본 템플릿이 IsSelected 배경/글자색을
                                 SystemColors 키로 직접 그리기 때문에, CellStyle의 Trigger로는 색을 바꿀 수 없다.
                                 드래그 선택 시 너무 어둡게 보이던 문제를 여기서 밝은 색 + 흰 글씨로 교체. -->
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#3A3A48"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#3A3A48"/>
                                <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White"/>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="항목" Binding="{Binding Key}" Width="Auto" MinWidth="140">
                                    <DataGridTextColumn.HeaderStyle>
                                        <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource DataGridHeaderStyle}">
                                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.HeaderStyle>
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="NoWrap"/>
                                            <Setter Property="TextAlignment" Value="Center"/>
                                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                                <DataGridTextColumn Header="값" Binding="{Binding Value}" Width="Auto" MinWidth="200">
                                    <DataGridTextColumn.ElementStyle>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="TextWrapping" Value="NoWrap"/>
                                            <Setter Property="VerticalAlignment" Value="Center"/>
                                        </Style>
                                    </DataGridTextColumn.ElementStyle>
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </HeaderedContentControl>
                </Grid>
            </TabItem>

            <!-- ═══ 변환기 (구 Converter 탭 통합: JWT/Base64/JSON/XML/cm↔px) ═══ -->
            <TabItem Header="변환기" Style="{StaticResource UtilTabStyle}">
                <views:ParserView DataContext="{Binding Parser}"/>
            </TabItem>

        </TabControl>
    </Grid>
</UserControl>
