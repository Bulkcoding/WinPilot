<UserControl x:Class="WinPilot.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="{DynamicResource BgBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="380">
            <Border Style="{StaticResource CardStyle}">
                <StackPanel HorizontalAlignment="Center">

                    <!-- 로고 -->
                    <TextBlock Text="WinPilot" FontSize="40" FontWeight="Bold"
                               Foreground="#F97316" HorizontalAlignment="Center"/>
                    <TextBlock Text="{Binding Version}" FontSize="14" Foreground="#64748B"
                               HorizontalAlignment="Center" Margin="0,4,0,0"/>

                    <Rectangle Height="1" Fill="{DynamicResource BorderBrush}" Margin="0,20,0,20"/>

                    <!-- 정보 그리드 -->
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="16"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="8"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="설명"      Style="{StaticResource InfoLabelStyle}"/>
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Description}" Style="{StaticResource InfoValueStyle}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="제작"      Style="{StaticResource InfoLabelStyle}"/>
                        <TextBlock Grid.Row="2" Grid.Column="2" Text="{Binding Author}"      Style="{StaticResource InfoValueStyle}"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="런타임"    Style="{StaticResource InfoLabelStyle}"/>
                        <TextBlock Grid.Row="4" Grid.Column="2" Text="{Binding Runtime}"     Style="{StaticResource InfoValueStyle}"/>

                        <TextBlock Grid.Row="6" Grid.Column="0" Text="빌드"      Style="{StaticResource InfoLabelStyle}"/>
                        <TextBlock Grid.Row="6" Grid.Column="2" Text="{Binding BuildDate}"   Style="{StaticResource InfoValueStyle}"/>
                    </Grid>

                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
